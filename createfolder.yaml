---
 - hosts: all
   tasks:
     - name: Collect current  timestamp
       shell: date +%d%b%y_%H-%M-%S
       register: timestamp
     - name: Create directory structure
       win_file:
         path: C:\Users\yash\Desktop\{{timestamp.stdout }}
         state: directory
